# Changelog

## 1.1.18

本次发布合并 1.1.17–1.1.18 的本地修正；1.1.17 未单独发布。

- Edge 网页/PDF 的可访问性接口返回空选区时，继续检查父节点并尝试原生复制，避免直接误用旧剪贴板。地址栏仍排除，已确认选区但复制失败时仍停止；其他应用仍只读取剪贴板。
- 修正 1.1.17 本地版新建 Edge 分屏后 S04 的回归：在创建分屏前记录原页面身份，随后重新取得控件时按原身份匹配，不把新出现的右侧入口再次列入来源排除范围。
- 来源控件重建时可按原网址匹配；已有分屏中的普通/未知页面及无法确认位置的文档继续保护。来源身份仅保存在本轮内存中，不写入日志。
- Edge 控件查找针对已知控件失效/暂时忙碌错误最多读取 3 次，每次重试前等待 80 毫秒并重新获取根控件；输入和发送不进入该重试。
- 日志增加选区判断结果、具体 COM 错误码，以及分屏入口候选数量，不记录选区正文。
- 162 项离线检查通过。新增回归用例已先在旧判断下失败，再于修复后通过；实际 Edge 网页/PDF 的完整 UI 流程尚未在本次验证，不能据此保证所有用户环境均已修复。
- 程序名称、原调用路径、鼠标按键/键盘快捷键用法和便携式日志保持不变；下载包不含个人日志。

## 1.1.16

本次发布合并 1.1.15–1.1.16 的本地更新。

- 跨应用调用先激活默认 Chrome/Edge 最靠前的窗口，核对当前可见页面；已是 DeepSeek 时复用原对话，避免连续查询反复新开标签页。
- 当前为独立空白标签页时，在该标签页直接打开 DeepSeek；普通网页才沿用新开 DeepSeek 页面的流程。浏览器未启动时先启动。
- 只检查当前页面及可见分屏，不遍历后台标签页，不根据标题跳到其他窗口；确认目标网址后才发送。
- 空白页导航沿用已有快速识别和地址栏二次确认，保留地址栏未提交输入及现有分屏。页面变化或识别失败时停止，不回退新开页面。
- 草稿和正在回答保护保持不变；非浏览器应用仍只读取剪贴板，不读取原应用选区或内容，不发送复制快捷键。
- README 补充 Windows 快捷方式的键盘快捷键配置，并统一鼠标按键与键盘快捷键的用法。
- 141 项离线检查通过，覆盖当前窗口选择、对话复用、空白页原位导航及失败不新开页面等分支。新增空白页复用流程尚待 Edge/Chrome 实机验证。
- EXE 名称、路径约定及便携式日志不变，发布包不含个人日志。

## 1.1.14

- 其他应用或桌面入口改为仅读取已有系统剪贴板；请先使用原应用的复制功能，再按鼠标键。
- 删除跨应用选区读取和复制逻辑，不读取原应用的选区、文本、控件树或终端输出，不发送复制快捷键，也不改写剪贴板。
- 仅保留区分浏览器入口及检查窗口切换所需的窗口句柄、进程标识和进程名，避免干扰 Linux/WSL/SSH 等终端。
- 浏览器内仍支持划词优先、无选区时使用剪贴板；其他应用调用默认浏览器后直接查询，保留窗口激活流程。
- README 区分浏览器与其他应用的使用步骤。
- 128 项离线检查通过，新增外部入口无复制动作、剪贴板短暂占用及空剪贴板检查；未新增跨应用实机验证。
- 程序名称、宏路径、便携式日志和 10 秒超时保护保持不变，发布包不含个人日志。

## 1.1.13

本次发布合并 1.1.12–1.1.13 的本地更新。

- 新增跨应用入口：优先读取原应用提供的文字选区，否则使用已有剪贴板，通过系统默认浏览器打开 DeepSeek 查询。
- 默认浏览器支持 Chrome/Edge；只传递固定网址，不将查询文字拼接到网址或命令行。
- 修复浏览器已打开但未到前台的问题：识别匹配默认浏览器安装路径的窗口，必要时还原并激活，确认前台后才输入。
- 多窗口时结合新窗口和标题变化识别候选；无法唯一确认时停止，不随意选择。
- 跨应用入口在 Chrome/Edge 均直接等待 DeepSeek，不经过新标签页判断和分屏创建；浏览器内调用保留原流程。
- README 更新完整用法，并澄清新标签页识别范围，不再使用容易误解的“兼容第三方搜索引擎新标签页”表述。
- 125 项离线检查通过；用户确认本机 Chrome 跨应用调用和前台激活可用。Edge 跨应用路径已作代码核对，尚未完成同等实机验证。
- 程序名称、宏路径、便携式日志和 10 秒超时保护保持不变，发布包不含个人日志。

## 1.1.11

本次发布合并 1.1.9–1.1.11 的本地更新。

- 新增剪贴板查询入口：有选区时优先复制，未选中时使用已有剪贴板文字；已知选区复制失败仍停止。
- 独立空白新标签页直接打开 DeepSeek 查询；普通网页/PDF 继续分屏，已有布局保持不变。
- 新标签页综合页面地址、原生地址栏及标题识别，覆盖部分 Chromium 内置新标签页地址形式及 Edge 在线新标签页；不代表任意第三方新标签页扩展均兼容。
- 优先走外层状态快速判断，不依赖选中标签控件及时更新；过渡状态间隔 80 毫秒复查，每轮最多额外等待 240 毫秒。
- 信息冲突以 N01 停止，不误入创建分屏流程；保留地址栏未提交输入。
- 移除启动固定 400 毫秒等待，改为按需等待鼠标/修饰键松开，不主动释放用户按键。
- README 已更新双入口用法。程序名称、宏路径和 EXE 旁 logs 目录不变，不增加常驻监听。
- 106 项离线检查通过，本次本地更新已获用户正向反馈；不同浏览器环境仍需持续验证。

## 1.1.8

- 用法更新为“选中文字后按一次鼠标键”：自动复制，再执行原有分屏、粘贴和发送流程。
- 复制前后核对剪贴板更新，读取期间发生变化时重试；不回退使用旧剪贴板内容。
- 复制等待上限 1.5 秒，包含在原有 10 秒超时保护内；未复制到文字或窗口切换时停止并报错。
- 程序名称、宏路径、已有分屏布局和便携式日志位置保持不变，不增加常驻监听。
- 52 项离线检查通过；新增自动复制的网页及 PDF 端到端兼容性仍需实测。

## 1.1.7

- 修复 Edge 多标签页下首次新建分屏可能在创建空白页之前停止的问题。
- 直接查询原生标签页；选中状态不唯一时使用窗口标题匹配来源，仍不明确则停止并提示。
- 首次新建分屏固定准备一个空白候选页，然后返回原阅读标签页，减少逐个查找候选页的开销。
- 原生控件扫描跳过标签栏内部子树，减少多标签页时触及扫描上限的情况。
- 增加创建空白页和返回阅读页的阶段记录，便于定位异常。
- 保留已有分屏布局、Chrome 流程及 EXE 旁的便携式日志目录。
- 48 项离线检查通过；本次 Edge 修复已获用户实机验证，其他环境仍需继续验证。

## 1.1.6

- 所有运行记录、去重指纹、维护提醒计时统一保存到 EXE 旁的 `logs` 文件夹。
- 不依赖 Codex、本机用户路径或启动工作目录；不可写时明确报 F01，不回退到其他目录。
- 每月提醒“打开运行记录”指向此便携式目录。
- 保留 GitHub 网页编辑的 README 文案，只更新版本和日志说明。
- 发布包不含运行记录；不自动迁移或覆盖旧版 AppData 日志。
- 44 项离线检查通过，包括目录位置、可写性探测清理和摘要落盘。

## 1.1.5

首个 GitHub 发布版本。

- Edge / Chrome：复制后按键启动，打开或复用原生分屏中的 DeepSeek，粘贴并发送原文。
- 保留已有布局，仅在新建分屏时调整比例。
- 成功静默退出，错误与 10 秒超时弹窗；单次发送、防重复和草稿保护。
- 每 30 天按需提醒检查本地摘要，记录上限 500 条。
- Edge 优化：批量读取原生控件属性，跳过隐藏页面、文档正文与标签页内部子树，短期缓存并在操作后失效；右侧入口定向搜索。
- Chrome 查询流程保持原有实现。

验证：40 项离线检查通过；基础功能经用户实测。最新 Edge 多标签页优化的实际性能仍待更广泛验证。
